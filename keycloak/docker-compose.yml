version: "3.9"

services:
  keycloak:
    build: .

    container_name: keycloak

    env_file:
      - .env

    ports:
      - "8081:8080"

    command:
      - start-dev

    restart: unless-stopped
